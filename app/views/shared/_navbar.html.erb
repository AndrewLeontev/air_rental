<nav class="navbar navbar-expand-md navbar-dark bg-dark mb-4">
  <a class="navbar-brand" href="/">AirPickachu</a>
  <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="navbar-collapse collapse" id="navbarCollapse" style="">
    <ul class="nav navbar-nav ml-auto">
      <% if !user_signed_in? %>
        <li class="nav-item active"><%= link_to t(:label_login), new_user_session_path, class: 'nav-link' %></li>
        <li class="nav-item active"><%= link_to t(:label_registration), new_user_registration_path, class: 'nav-link' %></li>
      <% else %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="dropdown03" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= image_tag avatar_url(current_user), class: 'bd-placeholder-img rounded-circle avatar-small' %>&nbsp;
            <%= current_user.fullname %>
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdown03" >
            <%= link_to t(:label_manage_listings), '#', class: 'dropdown-item' %>
            <%= link_to t(:label_list_user_space), '#', class: 'dropdown-item' %>
            <%= link_to t(:label_user_reservations), '#', class: 'dropdown-item' %>
            <%= link_to t(:label_user_trips), '#', class: 'dropdown-item' %>
            <div class="dropdown-divider"></div>
            <%= link_to t(:label_user_edit), edit_user_registration_path, class: 'dropdown-item' %>
            <%= link_to t(:label_user_logout), destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
          </div>
        </li>
      <% end %>
    </ul>

  </div>
</nav>